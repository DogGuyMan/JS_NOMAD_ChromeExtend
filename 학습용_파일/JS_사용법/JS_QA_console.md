# console API

## 1. console.dir()
지정된 JS 객체 속성에 대한 목록을 표시한다.

## 2. console.time() & timeEnd()
작업 소요 시간을 추적하는데 사용할 수 있는 타이머
```
console.time()
        : 
        (잡 코드)
        : 
console.timeEnd()
```